# MongoDB Collections are defined in database.py
# No SQLAlchemy models needed — MongoDB is schemaless
# Collections used:
#   - videos     : { title, url, youtube_id, order }
#   - shorts     : { title, url, youtube_id, order }
#   - posters    : { public_id, secure_url, order }
